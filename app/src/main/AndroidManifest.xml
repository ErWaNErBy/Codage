<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fr.codage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RSAActivity"/>
        <activity android:name=".MyEncryptionActivity" />
        <activity android:name=".DESActivity" />
        <activity android:name=".RectTranspoActivity" />
        <activity android:name=".HillActivity" />
        <activity android:name=".PlayfairActivity" />
        <activity android:name=".HomophoneActivity" />
        <activity android:name=".VigenereActivity" />
        <activity android:name=".CesarActivity" />
        <activity android:name=".AtbashActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>